/* ============================================================
   WHERE RSVP REPLIES GO
   ------------------------------------------------------------
   Paste the web-app link from your Google Sheet setup between
   the quote marks below. Step-by-step instructions are in
   RSVP-SETUP.md in this project folder.

   Until you paste it in, the form still works beautifully —
   it just tells guests to email you instead.
   ============================================================ */

export const RSVP_ENDPOINT = '';

/* Fallback address used if the link above is empty, or if the
   internet hiccups while a guest is submitting. */
export const RSVP_EMAIL = 'sarasvabek@gmail.com';
